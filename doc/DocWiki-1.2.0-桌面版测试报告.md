# DocWiki 1.2.0 桌面版测试报告

## 测试结论

- 测试日期：2026-06-22
- 测试环境：Windows 11 x64
- 测试版本：DocWiki 1.2.0
- 最终结论：通过，可发布
- 自动化测试：166 项通过，0 项失败
- 安装、启动、退出和卸载：通过

## 发布文件

- 安装包：`F:\trae_workspace\DocWiki-Setup-1.2.0.exe`
- 文件大小：82,494,083 字节
- 文件版本：1.2.0
- SHA-256：`40035C635CD147CA3190E793D018B12FC11DA3F66241C05792C718B4ED9B4453`
- 签名状态：未签名

## 自动化测试

| 测试组 | 通过 | 失败 |
| --- | ---: | ---: |
| API 与数据安全 | 49 | 0 |
| 前端状态与桌面逻辑 | 24 | 0 |
| 模型路由隔离 | 31 | 0 |
| Playwright 真实浏览器流程 | 62 | 0 |
| 合计 | 166 | 0 |

覆盖内容包括：文件树、搜索、任务创建与状态迁移、明日任务、AI 任务解析、文档创建/编辑/保存、字体颜色、历史版本、文件右键重命名/复制/移动/删除、Markdown 路径规范化、主题持久化、对话创建与删除，以及模型场景隔离。

## 模型联调

- DeepSeek 连接测试与模型列表获取通过。
- 小米 MiMo 连接测试与模型列表获取通过。
- 通用、快速响应和知识库问答按配置使用 DeepSeek。
- 视觉理解按配置使用 MiMo `mimo-v2-omni`。
- 普通对话不会自动切换到视觉模型或其他供应商。
- API 密钥仅写入隔离测试目录；测试结束后已删除，未进入源码或安装包。

## 桌面与安装测试

- Windows 窗口标题精确显示 `DocWiki`。
- 原生“文件/编辑/视图/帮助”菜单已移除。
- 首页首次启动显示 Hero 和智能体输入框，首页不显示右下角悬浮入口。
- 退出时显示“保存并退出 / 不保存退出 / 取消”；取消后应用继续运行。
- 安装向导显示版本 1.2.0，并允许选择安装目录。
- 使用独立目录 `F:\DocWiki-1.2.0-Test` 完成安装和启动验证。
- 首次启动在安装根目录创建 `data` 和 `.docwiki\state`。
- `data` 下自动创建项目、文献、报告、SOP、软件、写作、任务七个栏目。
- 新安装没有测试项目、测试任务或预置 Markdown 文档。
- 卸载向导正常完成，测试安装已清理。

## 主要修复确认

- 数据和状态不再写入 AppData，改为安装根目录。
- 任务保存不再生成冗余且乱码的 `任务清单.md`。
- AI 任务解析已接入模型结果，并兼容 Markdown 代码块中的 JSON。
- 查看任务支持直接编辑状态；已完成任务迁移到完成列表。
- 文档前后导航只识别当前栏目和真实文件，不再引用测试文件。
- 阅读、富文本和 Markdown 源码模式布局统一；图片可按正文宽度显示。
- 搜索、主题切换、历史版本和右键文件操作均已恢复。
- 对话模型严格遵循场景配置；文件跳转会打开并高亮准确文档。
- 空白对话无需删除；已有对话删除支持取消和确认后的正确状态切换。

## 发布提示

安装包未配置代码签名证书，Windows 可能显示未知发布者提示；这不影响本次功能测试结果。
